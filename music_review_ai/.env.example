OPENAI_API_KEY=sk-your-key
LLM_MODEL_NAME=gpt-4o-mini
HTSAT_MODEL_NAME=m-a-p/MERT-v1-95M
# Optional local download path (set after running scripts/download_htsat.py)
HTSAT_MODEL_PATH=
# Override sample rate if 필요 (기본적으로 모델 feature extractor에서 읽음)
# AUDIO_SAMPLE_RATE=24000
# ALLOWED_ORIGINS=["http://localhost:3000"]
# LLM_REQUEST_TIMEOUT=15
# LLM_MAX_RETRIES=2
# LLM_RETRY_BACKOFF=1.2
# API_HOST=0.0.0.0
# API_PORT=8000
# API_RELOAD=false
# API_WEB_CONCURRENCY=4
CLAP_MODEL_NAME=laion/clap-htsat-fused
# Optional local download path (set after running scripts/download_clap.py)
CLAP_MODEL_PATH=
CLAP_CACHE_DIR=.cache/clap
TAG_MODEL_NAME=MIT/ast-finetuned-audioset-10-10-0.4593
# Optional local download path (set after running scripts/download_tagger.py)
TAG_MODEL_PATH=
TAG_CACHE_DIR=.cache/tagger
